<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Black">
        <!-- Customize your theme here. -->
    </style>

    <style name="SpecialTextAppearance">
        <item name="android:textSize">@dimen/small</item>
    </style>

</resources>
